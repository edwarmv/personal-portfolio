---
import type { HTMLAttributes } from 'astro/types';

interface Props extends HTMLAttributes<'textarea'> {}

const { class: classList, ...props } = Astro.props;
---

<textarea class:list={['bg-surface p-2', classList]} {...props}></textarea>
