---
import { defaultLanguage } from '../i18n/default-lang-options';

export const prerender = false;

const locale = Astro.preferredLocale ?? defaultLanguage;

return Astro.redirect(`/${locale}`, 307);
---
